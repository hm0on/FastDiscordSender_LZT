# 🚀 Discord Scheduled Message Sender

<div align="center">

![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)
![Flet](https://img.shields.io/badge/Flet-0.80+-green.svg)
![Platform](https://img.shields.io/badge/Platform-Windows%20%7C%20macOS%20%7C%20Linux-lightgrey.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

**Простое и удобное приложение для отложенной отправки сообщений в Discord с точностью до миллисекунд**

[Установка](#-установка) • [Использование](#-использование) • [Сборка](#-сборка-в-exe) • [Особенности](#-особенности)

</div>

---

## 📋 Описание

Discord Scheduled Message Sender — это десктопное приложение с графическим интерфейсом, позволяющее отправлять сообщения в Discord каналы в точно указанное время. Приложение использует User Token для авторизации и обеспечивает высокую точность отправки благодаря оптимизированному алгоритму ожидания.

### ✨ Основные возможности

- ⏰ **Точная отправка** — сообщения отправляются в указанное время с точностью до миллисекунд
- 🎨 **Удобный интерфейс** — простой и интуитивный GUI на базе Flet
- 📝 **Поддержка переносов строк** — корректная обработка многострочных сообщений
- 🔄 **Автоматическая повторная попытка** — при неудаче выполняется повторная отправка
- 💻 **Кроссплатформенность** — работает на Windows, macOS и Linux
- ⚡ **Оптимизированная производительность** — минимальная нагрузка на CPU

---

## 🎯 Особенности

### Точность отправки
- Оптимизированный алгоритм ожидания с точностью до миллисекунд
- Умное управление задержками для снижения нагрузки на CPU
- Автоматическое определение следующего дня, если указанное время уже прошло

### Безопасность
- Токен пользователя скрыт при вводе (поле с паролем)
- Все данные обрабатываются локально
- Нет передачи данных на сторонние серверы

### Удобство использования
- Простой и понятный интерфейс
- Отображение статуса в реальном времени
- Поддержка многострочных сообщений с переносами строк

---

## 📦 Установка

### Требования

- Python 3.8 или выше
- Интернет-соединение для отправки сообщений в Discord

### Шаги установки

1. **Клонируйте репозиторий** (или скачайте архив):
   ```bash
   git clone <repository-url>
   cd atystrem
   ```

2. **Установите зависимости**:
   ```bash
   pip install -r requirements.txt
   ```

   Или вручную:
   ```bash
   pip install flet requests
   ```

3. **Запустите приложение**:
   ```bash
   python main.py
   ```

---

## 🚀 Использование

### Получение User Token

⚠️ **Важно**: Для работы приложения необходим User Token (не Bot Token!).

**Как получить User Token:**
1. Откройте Discord в браузере (Chrome/Firefox)
2. Нажмите `F12` для открытия DevTools
3. Перейдите во вкладку `Network` (Сеть)
4. Обновите страницу (`F5`)
5. Найдите любой запрос к `discord.com/api`
6. Откройте вкладку `Headers` (Заголовки)
7. Найдите заголовок `Authorization` и скопируйте значение (это ваш токен)

⚠️ **Внимание**: Никому не передавайте свой токен! Это может привести к компрометации аккаунта.

### Получение Channel ID

1. В Discord включите режим разработчика: **Настройки** → **Расширенные** → **Режим разработчика**
2. Правой кнопкой мыши на канале → **Копировать ID канала**

### Использование приложения

1. **Запустите приложение**:
   ```bash
   python main.py
   ```

2. **Заполните форму**:
   - **Токен пользователя**: Вставьте ваш User Token
   - **Server ID**: (необязательно) ID сервера
   - **Channel ID**: ID канала, куда отправить сообщение
   - **Время отправки**: Время в формате `HH:MM` (например, `14:30`)
   - **Сообщение**: Текст сообщения (поддерживаются переносы строк)

3. **Нажмите "Start"** и дождитесь отправки

4. **Статус выполнения** отображается в реальном времени:
   - `Ожидание: X сек.` — обратный отсчет до отправки
   - `Отправка сообщения...` — идет отправка
   - `Сообщение успешно отправлено` — успешная отправка
   - `Ошибка отправки, код: X` — ошибка при отправке

---

## 🔧 Сборка в exe
(готовая сборка уже лежит в releases)

### Быстрый старт

**На Windows:**
```bash
flet build windows --project "DiscordSender"
```

**На macOS/Linux (для создания Windows exe):**
```bash
pip install pyinstaller
pyinstaller --onefile --windowed --name "DiscordSender" main.py
```

Готовый exe файл будет в папке `dist/` или `build/windows/`.

---

## 📁 Структура проекта

```
project/
├── main.py              # Основной код приложения
├── requirements.txt    # Зависимости проекта
├── README.md           # Этот файл
└── dist/               # Собранные exe файлы (после сборки)
```

---

## 🛠️ Технические детали

### Используемые технологии

- **Flet** — современный фреймворк для создания GUI приложений на Python
- **Requests** — библиотека для HTTP запросов к Discord API
- **Threading** — многопоточность для фоновой обработки

### Алгоритм точной отправки

1. **Парсинг времени**: Время в формате `HH:MM` преобразуется в timestamp
2. **Оптимизированное ожидание**: 
   - При большом времени ожидания (>50 мс) используются интервальные задержки
   - В финальной фазе (<50 мс) применяется точное ожидание с минимальными задержками
3. **Отправка**: HTTP POST запрос к Discord API v9
4. **Повторная попытка**: При неудаче выполняется немедленная повторная отправка

### API Discord

Приложение использует официальный Discord API v9:
- Endpoint: `https://discord.com/api/v9/channels/{channel_id}/messages`
- Метод: `POST`
- Авторизация: User Token в заголовке `Authorization`

---

## ⚠️ Ограничения и предупреждения

- **User Token**: Использование User Token может нарушать Terms of Service Discord. Используйте на свой риск.
- **Rate Limits**: Discord имеет ограничения на частоту запросов. Не злоупотребляйте.
- **Безопасность**: Никогда не передавайте свой токен третьим лицам.
- **Точность**: Точность отправки зависит от системных часов и загрузки системы.

---

## 🐛 Известные проблемы

- При первом запуске может потребоваться время на инициализацию GUI
- Некоторые антивирусы могут блокировать собранные exe файлы (ложное срабатывание)

---

<div align="center">

**Сделано с ❤️ для удобной работы с Discord**

⭐ Если проект был полезен, поставьте звезду!

</div>
